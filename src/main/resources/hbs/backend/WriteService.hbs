package {{packagePath}}.service;

import io.terminus.common.model.Response;
import {{packagePath}}.model.{{className}};

/**
 * Code generated by terminus code gen
 * Desc: {{comment}}写服务
 * Date: {{now}}
 */
public interface {{className}}WriteService {

    /**
     * 创建{{className}}
     * @param {{paramName}}
     * @return 主键id
     */
    Response<Long> create{{paramUpperFirst}}({{className}} {{paramName}});

    /**
     * 更新{{className}}
     * @param {{paramName}}
     * @return 是否成功
     */
    Response<Boolean> update{{paramUpperFirst}}({{className}} {{paramName}});

    /**
     * 根据主键id删除{{className}}
     * @param {{paramName}}Id
     * @return 是否成功
     */
    Response<Boolean> delete{{paramUpperFirst}}ById(Long {{paramName}}Id);
}